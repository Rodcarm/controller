name: Save in repo
on:
  push:
    branches:
      - main
jobs:
  update-repo:
    runs-on: ubuntu-latest
    steps:
      - name: get files in txt
        run: |
          contenido=$(tr '\n' ' ' < controller/files/new-files/archivesToTest.txt)
          echo "$contenido"